name: Revisar acuerdos TSJ

on:
  schedule:
    - cron: "55 21 * * 1-5"

  workflow_dispatch:

jobs:
  revisar-acuerdos:
    runs-on: ubuntu-latest

    steps:
      - name: Llamar endpoint de revisión
        run: |
          echo "Ejecutando revisión de acuerdos..."
          curl --fail --show-error --location --max-time 120 -X GET "${{ secrets.RENDER_MONITOR_URL }}"